extends layout

block content
  each item in scripts.Scripts
    br
    h3 Name: #{item.Name}
    h4 Description: #{item.Description}
    h4 Notes: #{item.Notes}
    h4 Tags: #{item.Tags}
    
    a(class='button', href="/scripts/details/" + item.Name) Run #{item.Name}

